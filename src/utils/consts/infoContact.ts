const infoContact = {
  email: process.env.NEXT_PUBLIC_EMAIL,
  phone: process.env.NEXT_PUBLIC_PHONE,
  location: process.env.NEXT_PUBLIC_LOCATION,
};

export const { email, phone, location } = infoContact;